/**
 * Voice.Assembly.FM Server Configuration
 * Centralized configuration for all server components
 */

export const config = {
  signalingPort: 8000,
  ctrlClientPort: 8080,
  synthClientPort: 8081,
};